# Backend API base URL (no trailing slash)
VITE_API_URL=http://localhost:8000

# Password for /key endpoint (must match server KEY_PASSWORD)
# Used by frontend to fetch Gemini API key from backend. Keep strong.
VITE_GEMINI_KEY_PASSWORD=
